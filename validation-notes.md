# 증빙 파일 업로드 검증 기록

## 2026-08-27

화주 포털에서 **건별 CS 접수**를 열어 파손 사진, 파손 영상, 판매가격 증빙 이미지 업로드 영역을 확인했습니다. 세 개의 파일 입력에 유효한 PNG·MP4 파일을 각각 전달한 뒤, 첨부 수가 `첨부 3/8`로 갱신되었고 영상·판매가 증빙 파일명 미리보기가 표시되는 것을 확인했습니다.

각 증빙 유형의 클라이언트와 서버 검증 기준은 아래와 같습니다.

| 증빙 유형 | 허용 형식 | 개수 | 파일당 제한 |
| --- | --- | ---: | ---: |
| 파손 사진 | JPG, PNG, WEBP | 최대 5개 | 7MB |
| 파손 영상 | MP4, MOV | 최대 2개 | 30MB |
| 판매가격 증빙 | JPG, PNG, WEBP | 최대 1개 | 7MB |

유효한 파일을 세 종류로 하나씩 선택했을 때 첨부 카운터가 `3/8`로 변경되었고, 사진은 썸네일로, 영상과 판매가격 증빙은 파일명·용량 행으로 표시되었습니다. `application/pdf` 형식의 파일을 파손 사진 입력으로 전달했을 때, 기존 사진 미리보기 수와 첨부 카운터가 변하지 않아 허용 형식 제한이 동작하는 것을 확인했습니다.

실제 S3 저장은 로그인한 화주 담당자의 요청으로만 실행되도록 보호된 업로드 계약에 연결되어 있습니다. 인증되지 않은 브라우저에서는 기존 로그인 흐름으로 이동합니다.

## 다중 담당자·증빙 검토 워크플로

대리점 관제 화면에서 각 티켓의 **화주 작성자**, 추가 참여 담당자 수, 그리고 대리점 담당자 배정 선택기를 확인했습니다. 선택된 티켓에는 화주가 등록한 파손 사진 2개, 파손 영상 1개, 판매가 증빙 1개의 열람·개별 다운로드·전체 다운로드 동선이 표시됩니다.

보상 검토 버튼을 통해 별도 다이얼로그가 열리며, 증빙 검토 완료 상태와 승인 또는 반려 선택지를 제공합니다. 반려를 선택하면 반려 사유 입력이 필수이고, 두 결정 모두 화주 참여 담당자에게 결과가 공유되도록 안내합니다.

반려 옵션을 선택한 뒤 반려 사유를 입력하여 제출했을 때, 다이얼로그가 닫히고 `보상 반려 사유를 등록했습니다. 화주 참여자에게 안내합니다.`라는 성공 토스트가 표시되는 것을 확인했습니다.

대리점 담당자 배정 선택기는 `박매니저 · 사고 보상팀`으로 변경 가능한 상태임을 확인했습니다. 브라우저 자동화의 선택 동작 반환에는 문자열 인코딩 오류가 발생했으나, 대체 확인을 통해 선택값 변경 자체는 반영되었습니다.

변경된 담당자 배정은 티켓 상세 메타데이터에 `박매니저 · 사고 보상팀`으로 표시되었습니다. 또한 개별 파손 사진의 열람 버튼을 선택했을 때, 파일 유형·파일명·등록자·등록시각·다운로드 동작을 포함하는 증빙 미리보기 다이얼로그가 열리는 것을 확인했습니다.

보상 승인 확정 후에는 `보상 승인 상태로 변경했습니다. 화주 참여자에게 알림을 보냅니다.` 성공 토스트가 표시되었습니다. 개별 증빙 다운로드 동작도 `파손부위_01.jpg 다운로드를 준비했습니다.` 토스트로 피드백되는 것을 확인했습니다.

## 타임라인·고급 검색·라이트박스

대리점 티켓 상세 하단에서 접수, 자동 배정, 내부 코멘트, 상태 변경, 보상 검토 순서의 통합 활동 타임라인을 확인했습니다. **보상 처리 대기** 빠른 필터를 선택하자 티켓 목록이 5건에서 보상 대기 상태의 1건으로 즉시 축소되었습니다.

고급 검색 버튼을 선택하면 담당자와 보상 처리 상태를 독립적으로 선택하고 초기화할 수 있는 패널이 표시됩니다. 파손 사진의 **크게 보기**를 선택했을 때, 어두운 배경의 라이트박스에서 확대·축소(70%~200%), 닫기, 원본 다운로드 제어를 포함한 고해상도 증빙 확인 화면이 열리는 것을 확인했습니다.

라이트박스의 확대 버튼을 선택했을 때 확대율이 `100%`에서 `125%`로 갱신되고, 증빙 미리보기 영역이 함께 확대되는 것을 확인했습니다.

고급 검색에서 담당자를 `김대리 · 운영 2팀`으로 선택한 결과, 이미 적용된 `보상 대기` 조건과 결합되어 두 조건을 모두 만족하는 티켓이 없는 경우 목록이 `0`건으로 표시되는 것을 확인했습니다. 이는 고급 조건이 빠른 필터와 함께 AND 조건으로 적용됨을 보여줍니다.

## 공개 랜딩 및 초대 온보딩

공개 랜딩에서 서비스 소개, 대리점 시작 CTA, 초대 링크를 통한 자동 소속 연결, 증빙·정산 정보 접근 제어를 안내하는 섹션이 정상 렌더링됐습니다. 대리점 가입 화면에서는 대리점명·사업자 등록번호·대표 담당자·연락처·업무 이메일의 입력 필드를 확인했고, 필수 정보를 입력할 수 있음을 확인했습니다.

대리점 가입 필수값 입력 후 제출했을 때 초대 링크 생성 및 기존 화주 공지 준비 화면으로 정상 전환되었습니다. 문자 내용 검토 화면에서는 초대 대리점명·자동 소속 연결 안내·전환 공지·전용 링크를 표시하고, 실제 발송이 SMS 서비스 연결 및 최종 확인 이후에만 진행됨을 고지하는 것을 확인했습니다.

초대 링크 흐름 미리보기에서 화주 등록 화면으로 이동했을 때 초대 코드와 자동 소속 연결 안내가 표시되었습니다. 화주 기본 정보, 복수 담당자 추가, 정산 은행·예금주·계좌번호, 직인 이미지 업로드, 민감정보 이용 동의 필드를 한 화면에서 확인했습니다.

화주 가입 화면의 필수 기본정보, 담당자 정보, 예금주 및 계좌번호 입력과 정산 은행 선택이 정상 반영되는 것을 확인했습니다. 이 화면은 정산·직인이 제한된 접근으로 관리되며 이용 동의가 필요함을 명확히 표시합니다.

필수 정보 입력과 민감정보 이용 동의 후 화주 등록 완료를 선택했을 때, 화주 정보가 대리점 워크스페이스에 연결되었다는 성공 토스트와 자동 소속 연결 완료 화면이 표시되는 것을 확인했습니다.

별도 초대 링크에서 **담당자 추가**를 선택했을 때 기존 입력 행을 보존한 채 담당자 2의 성명·부서/역할·휴대폰 번호 입력 행이 추가되는 것을 확인했습니다.

직인 파일 선택 이벤트 후 직인 업로드 영역이 `company-seal.png`와 `등록 준비 완료 · 다른 파일을 선택하면 교체됩니다.` 상태로 전환되는 것을 확인했습니다. 복수 담당자 입력 행도 유지되어 여러 화주 담당자 등록을 위한 UI 동작이 확인되었습니다.

화주 초대 관리 화면에서 **복사**를 선택했을 때 전용 가입 링크가 클립보드에 복사되고 `초대 링크를 복사했습니다.` 성공 알림이 표시되는 것을 확인했습니다.

**새 링크 생성**을 선택했을 때 초대 토큰이 갱신되고 `새 화주 초대 링크를 생성했습니다.` 토스트가 표시됐습니다. 이후 공지 검토 화면에 갱신된 링크가 반영되었고, 선택된 3개 화주에 대한 발송 준비 버튼이 표시되는 것을 확인했습니다.

공지 검토 화면의 **3개 화주에게 발송 준비**를 선택했을 때 실제 외부 문자 발송 없이 `3개 화주의 공지 발송 요청을 준비했습니다.` 알림이 표시되고 버튼이 `발송 준비 완료`로 갱신되는 것을 확인했습니다.

## 운영 관리 확장 화면

대리점 콘솔에서 화주 목록 화면을 열었을 때 화주별 코드, 등록 담당자 수, 진행 CS, 보상·배송 상태, 최근 활동 정보를 테이블로 확인했습니다. 화주명·코드·담당자 검색, 상태 기준 필터, 화주 초대 및 목록 내보내기 진입 UI가 정상 표시됐습니다.

업무 보고서 화면에서는 기간 선택, 처리 완료·응답 시간·보상 검토·SLA 주의 KPI, 일별 유입/완료 막대 그래프, 당일 확인 항목, 운영 요약을 확인했습니다. 대리점 설정 화면에서는 대리점 기본정보, CS 응답 기준, 공지 발송 권한 안내 및 조직·담당자 권한/화주 조직/문서 정책 탭이 정상 표시됐습니다.

조직 및 담당자 권한 탭에서 대리점과 화주 소속을 함께 표시하는 담당자 목록, 업무별 권한 칩, 담당자 추가 동선을 확인했습니다. 화주 CS 담당자의 티켓 관리 권한을 선택했을 때 권한 상태가 갱신되고 `박지수의 티켓 관리 권한을 변경했습니다.` 알림이 표시되는 것을 확인했습니다.

문서·직인 정책 탭에서 화주 등록 직인 자동 적용, 직인 사용 이력 기록, 직인 적용 권한 정책이 표시됐으며 내부 운영 미리보기와 실제 문서 환경의 보안 적용 범위를 분리해 안내하는 것을 확인했습니다. 파손 티켓의 보상 검토에서 승인을 선택하면 `합의서 생성·직인 적용` 단계로 진행하도록 구현된 것을 확인했습니다.

합의서 생성 단계에서 화주명·사고유형·지급금액·직인 출처가 포함된 문서 미리보기가 열렸습니다. `등록 직인 자동 적용`을 선택했을 때 화주 서명란에 직인 인상이 표시되고 `화주 등록 직인을 합의서 미리보기에 적용했습니다.` 성공 알림이 표시되는 것을 확인했습니다.

화주 초대 관리 화면에서 공지 문자 내용을 검토하고, 발송 준비 완료 후 수신자·내용·발송 권한을 다시 보여 주는 최종 확인 모달을 확인했습니다. 최종 입력란에 `발송`을 입력하기 전에는 발송 확정 버튼이 비활성화되고, 일치하는 문구를 입력한 뒤에는 `3건 문자 발송 확정` 버튼이 활성화되는 것을 확인했습니다. 실제 SMS 서비스가 연결되지 않은 검토 모드임을 화면에 명시했습니다.

화주 직인 이미지가 선택되면 브라우저 세션의 등록 직인 자산으로 저장되고, 보상 합의서의 직인 출처 미리보기와 화주 서명란에 같은 이미지가 자동 적용되는 것을 확인했습니다. 자동 날인 후 `합의서 생성 및 확정`을 실행했을 때 `보상 합의서를 생성하고 직인 적용 이력을 기록했습니다.` 완료 알림과 함께 합의서 모달이 종료되는 것을 확인했습니다.

연동 전 검토 모드에서 최종 확인 문구 `발송`을 입력한 뒤 `3건 문자 발송 확정`을 실행했을 때 `3개 화주의 공지 발송 요청을 최종 확인했습니다.` 알림이 표시됐습니다. 모달은 `발송 요청이 기록되었습니다.` 상태와 SMS 서비스 연결 후 전송 결과를 이력에 반영한다는 안내로 전환됐으며, 실제 외부 문자 발송은 실행되지 않았습니다.

직인 업로드는 인증된 화주 계정의 `shipper_seals` 테이블에 S3 키·제공 URL·파일명·MIME 타입·용량을 저장하도록 연결했습니다. 라우터 단위 테스트에서 허용된 직인 이미지가 S3 저장 호출과 메타데이터 upsert를 함께 수행하는 것을 검증했고, 합의서 화면은 업로드 응답의 저장 URL을 브라우저 세션에 연결해 동일 자산을 직인 출처와 서명란에 사용합니다.

로그인하지 않은 초대 등록 화면에서 유효한 직인 이미지가 선택되면 파일 미리보기와 함께 `로그인 후 직인 보안 저장` 안내가 표시되는 것을 확인했습니다. 로그인된 화주 계정에서는 선택한 파일이 보호된 업로드 절차를 통해 S3와 `shipper_seals` 메타데이터에 저장되며, 업로드 응답의 저장 URL이 합의서에 전달되도록 구현했습니다.

## 화주 완료 이력 및 화주 관리 보완

화주 포털에서 **CS 유형 요약**과 `처리 완료` 탭을 확인했습니다. 요약 영역은 최근 30일 기준 배송지연·파손/분실·오배송·주소변경의 접수 건수와 상대 비중, 우선 확인 맥락을 제공합니다. 처리 완료 탭은 티켓번호, 송장번호, 수령인, 문의 유형, 처리 결과, 담당자, 완료 일시를 분리해 표시합니다.

대리점 화주 목록은 기존 운영 상태 열을 제거하고 화주별 **주력 상품 카테고리와 세부 카테고리**를 표시하도록 개편했습니다. 목록의 `화주 초대` 버튼은 자동 소속 연결 흐름과 링크 복사·재생성 기능을 갖춘 생성 모달을 열었고, 업체 행은 사업장·대표 연락처·복수 CS 수신 담당자·월 출고 건수·카테고리별 운영 메모가 있는 상세 프로필로 정상 전환되었습니다.

직인 등록 화면은 비로그인 상태에서 보안 저장을 위해 로그인하도록 안내하며, 업로드 요청 실패 시 오류 사유를 인라인으로 표시하고 동일한 유효성 검사를 다시 통과한 가입 폼을 즉시 재제출할 수 있는 `다시 시도` 동작을 제공합니다. 타입 검사, 서버 단위 테스트 6건, 프로덕션 번들 생성 및 데스크톱 렌더링을 확인했습니다. 실제 로그인 계정으로 S3 저장 자산을 다시 조회해 합의서에 날인하는 종단 간 검증은 별도 인증 세션에서 수행해야 합니다.

## 대리점·화주 담당자 계정 설정 및 로그인

대리점과 화주 모두 회사 소속을 확인하는 **사업자등록번호**와 개인별 콘솔 접근 수단인 **담당자 아이디·비밀번호**를 분리했습니다. 대리점 가입을 마치면 대표 운영자 계정 설정 화면으로 이동하며, 화주 초대 화면에는 화주 담당자의 계정 설정·기존 로그인 진입을 함께 표시합니다.

계정 설정은 대리점 운영자·화주 담당자 역할을 명시적으로 선택하고, 조직명·사업자등록번호·담당자명·로그인 아이디·비밀번호 확인을 받습니다. 비밀번호 원문은 저장하지 않고 고유 salt가 포함된 scrypt 해시만 보관하며, 성공한 등록 또는 로그인은 기존 보호 세션 쿠키를 발급해 콘솔 및 직인 저장 절차로 이어집니다. 로그인 화면에서는 사업자등록번호와 등록 담당자명을 대조해 마스킹된 아이디만 확인할 수 있습니다.

화주 담당자와 대리점 운영자 계정 설정 화면, 로그인 및 화주 초대 화면의 계정 설정 안내를 데스크톱 렌더링으로 확인했습니다. `pnpm check`, `pnpm test`(9건), `pnpm build`가 통과했습니다. 실제 다수 담당자에게 초대 링크를 발송하고 각 담당자가 자신의 계정을 활성화하는 조직 멤버십·초대 토큰 영속화는 후속 기능으로 남아 있습니다.

초대 토큰은 대리점 운영자가 지정한 화주사명·사업자등록번호·14일 유효기간·상태와 함께 저장합니다. 화주 담당자 계정 설정은 유효한 초대 토큰과 초대 대상 사업자등록번호가 일치할 때만 허용하며, 성공 후 해당 초대 토큰을 사용 처리합니다. 대리점 초대 화면의 대상 화주사·사업자등록번호 입력, 발급 전 빈 상태, 화주 계정 설정의 사업자등록번호 대조 안내를 렌더링으로 확인했습니다. 서버 단위 테스트에는 일치 시 계정 생성·초대 사용 처리와 불일치 시 `FORBIDDEN` 거부를 포함했으며, 전체 10건 테스트와 빌드가 통과했습니다.

## 초대 정보 자동 적용 및 인증 직인 E2E

대리점 테스트 계정으로 화주명 `테스트화주` 및 사업자등록번호 `1234567898`를 지정해 전용 링크를 발급했습니다. 화주가 해당 링크에서 계정 설정을 열자 회사명과 사업자등록번호가 **읽기 전용 초대 정보로 자동 적용**되었고, 담당자명·아이디·비밀번호만 추가 입력하도록 확인했습니다. 화주 가입 폼에서도 같은 초대 정보가 자동 표시되며 중복 입력 항목은 제거되었습니다.

테스트 화주 담당자 계정으로 온보딩을 완료하며 PNG 직인을 업로드했습니다. 서버는 해당 직인을 S3의 `shipper-seals/{userId}/...` 경로에 저장하고, `shipper_seals` 메타데이터에 파일명·MIME 유형·크기·URL을 기록했습니다. 이어 보상 합의서 미리보기에서 저장된 직인을 조회했고, `등록 직인 자동 적용`으로 화주 서명란의 날인 상태와 성공 안내를 확인했습니다. 이 검증에는 비민감 테스트 이미지만 사용했습니다.

로그인 화면에서 테스트 화주 담당자의 아이디·비밀번호 로그인 후 콘솔 진입을 확인했고, 사업자등록번호와 담당자명으로 조회할 때 아이디가 마스킹되어 표시됨을 확인했습니다. 대리점 설정의 `조직 및 담당자 권한` 탭은 `화주 담당자 초대`, `대리점 담당자 추가` 진입을 제공하며, 화주 조직 관리의 업체별 `담당자 초대`도 동일한 흐름으로 연결됩니다.

추가 화주 담당자 초대에서는 초대 링크를 실제 발급하고 접속했습니다. 계정 활성화 화면은 화주사명·사업자등록번호·초대 담당자명을 읽기 전용 정보로 자동 적용하며, 수신자는 개별 아이디와 비밀번호만 입력합니다. 활성화 완료 후 콘솔로 자동 이동하는 것까지 확인했습니다. 따라서 대리점이 초대할 때 입력한 회사 식별 정보는 화주와 추가 담당자가 다시 입력하지 않습니다.

대리점 가입 화면에서 비민감 테스트 대리점 정보를 제출한 뒤, 대표 운영자 계정 설정 화면으로 자동 전환되는 것을 확인했습니다. 사업자등록번호·대표 담당자명·개별 아이디·비밀번호를 입력해 계정을 생성하자, 대리점 전용 화주 초대 화면으로 자동 이동했습니다. 이 흐름에서 대표 운영자는 이후 화주명과 사업자등록번호를 한 번만 지정해 전용 초대 링크를 발급할 수 있습니다.

## 조직별 권한·실제 화주 운영 이력·계정 오류 UX

화주 담당자 계정으로 대리점 전용 `/agency/invites` 경로에 직접 접근했을 때, 화주 초대 기능은 대리점 계정 전용이라는 권한 불일치 안내와 `내 업무 공간으로` 이동 버튼이 표시되었습니다. 대리점 전용 초대 화면과 역할별 담당자 초대 화면은 모두 로그인 조직 유형을 확인한 후에만 본문을 렌더링합니다. 콘솔은 대리점 계정에만 CS 관리·화주 목록·화주 운영 이력·보고서·설정을 보이고, 화주 계정에는 자기 회사의 CS·배송 위험·처리 완료 화면만 보이도록 유지됩니다.

대리점 테스트 계정으로 `이력검증화주`의 전용 초대를 실제로 발급한 후, 화주 운영 이력 대시보드가 이 초대 토큰 기반의 대기 상태·만료 예정일·등록 담당자 수·정산 미등록 상태를 표시하는 것을 확인했습니다. 정산 계좌가 등록되면 계좌 원문은 AES-256-GCM 방식으로 암호화해 보관하고, 운영 화면에는 은행명과 마지막 네 자리만 표시합니다. 문서 날인 이벤트도 문서번호·이벤트·일시만 기록하며 직인 원본은 노출하지 않습니다. 이력이 없을 때의 빈 상태와 이력 조회 오류 시 재시도 화면도 구현했습니다.

로그인 화면에는 담당자 개인 계정 원칙, 아이디·비밀번호 입력 도움말, 비밀번호 표시 전환, 실패 인라인 경고, 별도 아이디 찾기 진입을 제공했습니다. 아이디 찾기 화면은 사업자등록번호 자릿수와 담당자명 누락을 명확히 안내하며, 일치하지 않는 경우 계정 정보를 노출하지 않습니다. `pnpm check`, `pnpm test`(16건), `pnpm build`가 통과했습니다.

## 실제 정산·문서 날인 이력 및 역할 전용 경로 재검증

대리점 테스트 계정으로 `이력검증화주` 초대를 발급한 뒤, 해당 화주 담당자 계정을 활성화했습니다. 화주 온보딩에서 신한은행 계좌와 비민감 테스트 직인을 제출한 결과, 계좌 원문은 암호화된 값으로 저장되고 운영 이력에는 `신한은행 · ****8821`만 표시되었습니다. 연결 대리점이 합의서 미리보기에서 등록 직인을 적용하고 확정하자, 감사 이력에 `LF-CA-20260827-021`의 **직인 적용**과 **합의서 확정** 이벤트가 각각 기록되었습니다. 대시보드는 초대 완료, 담당자 1명, 정산 계좌 검토, 문서 날인 2건을 실제 조회 결과로 표시했습니다.

화주 계정으로 대리점 초대 경로를 열었을 때와 대리점 계정으로 화주 담당자 초대 경로를 열었을 때 모두 권한 불일치 안내와 콘솔 복귀 경로가 표시되는 것을 확인했습니다. 담당자 초대 가드는 Wouter 경로 값이 아닌 현재 주소의 역할 쿼리를 판별하도록 수정해 교차 접근을 차단합니다. 대리점이 화주 직인을 문서에 적용할 때도 초대 토큰의 연결 관계를 서버에서 검증합니다. `pnpm check`, `pnpm test`(17건), `pnpm build`가 통과했습니다.

로그아웃한 비로그인 상태에서 `/agency/invites` 및 `/staff-invite?role=shipper`를 각각 직접 열어 확인했습니다. 두 경로 모두 업무 폼을 렌더링하지 않고, 해당 조직 역할에 맞는 개인 계정 로그인을 안내하는 `SIGN IN REQUIRED` 화면과 로그인 진입 버튼만 노출했습니다.

## 대리점 로그인 보강 검증

배포 주소 `https://logiflowcs-kjcctfmp.manus.space/login?returnTo=%2Fconsole`에서 `test.agency.owner` 계정으로 로그인해 대리점 콘솔의 OPERATIONS 화면과 대리점 전용 메뉴를 확인했습니다. 로그인 성공 후 `/console`로 이동하며, 대리점 역할에 맞는 티켓·배송 리스크·화주 SLA·화주 목록·업무 보고서·대리점 설정 메뉴가 표시됩니다. 로그인 화면의 개발 미리보기 계정 안내는 `import.meta.env.DEV` 조건으로만 렌더링되어 운영 배포에는 자격 증명이 노출되지 않습니다. 로그인 성공 시 인증 프로필과 조직별 대시보드 쿼리 캐시를 갱신하도록 보강했으며, `pnpm check`, `pnpm test` 21건, `pnpm build`를 통과했습니다.
